let room1 = "conference_room_a"; // "conference_room_a" (따음표 안)은 리터럴 입니다.

let currentRoom = room1; // 이제 currentRoom의 값은 room1의 값 ("conference_room_a")과 같습니다.

currentRoom = conference_room_a; // 에러가 일어납니다. conference_room_a 란 식별자는 존재하지 않습니다.